generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model JournalEntry {
  id           String   @id @default(uuid())
  content      String
  emotionLevel Int
  timestamp    DateTime @default(now())
}